import React from 'react';
import styled from 'styled-components';

const StyleTitle = styled.h1`
  text-align: center;
  font-weight: 400;
`;

const Title = () => <StyleTitle>Welcome to CodeJudge</StyleTitle>;

export default Title;
